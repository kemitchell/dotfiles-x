#!/bin/zsh
WIDTH=$(screen-size | awk -F x '{ print $1 }')

if [ "$WIDTH" -gt "1900" ]; then
  DIMEN="1920x1055"
else
  DIMEN="1600x875"
fi

rdesktop -x 80 -u "$1" -p "$2" windows.kemitchell.com -g "$DIMEN" -r clipboard:PRIMARYCLIPBOARD -z
